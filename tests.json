{
  "tests": [
    {
      "name": "Basic WAN Two-Stage T2V",
      "description": "Test cold start + simple video gen avec workflow two-stage",
      "command": "python test_client.py --endpoint $RUNPOD_ENDPOINT_ID --api-key $RUNPOD_API_KEY --prompt 'Simple test woman, NSFW, dynamic' --seed 42",
      "expected_output": "status: completed, metrics.total_s < 600, outputs[].type: video",
      "timeout": 600,
      "required_env": ["RUNPOD_API_KEY"]
    },
    {
      "name": "LoRA Integration Check",
      "description": "Test avec LoRA action (fem_mast_v1) sur network storage",
      "command": "python test_client.py --endpoint $RUNPOD_ENDPOINT_ID --api-key $RUNPOD_API_KEY --prompt 'Action sequence with LoRA' --lora 'action-lora/fem_mast_v1.safetensors' --seed 123",
      "expected_output": "status: completed, models_ok.wan: true, models_ok.lora: true",
      "timeout": 600,
      "required_env": ["RUNPOD_API_KEY"]
    },
    {
      "name": "Cold Start Metrics",
      "description": "Vérif cold start <30s + cost estimate",
      "command": "python test_client.py --endpoint $RUNPOD_ENDPOINT_ID --api-key $RUNPOD_API_KEY --prompt 'Quick cold start test' --seed 0",
      "expected_output": "metrics.cold_start_s < 30, cost_usd < 0.10",
      "timeout": 300,
      "required_env": ["RUNPOD_API_KEY"]
    }
  ]
}